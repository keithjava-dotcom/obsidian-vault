---
date: 2026-08-12
type: 每日精华
---

# 2026-08-12 每日精华

> 状态：几乎全自动化日 · 仅 1 次人类互动 · 136 条消息

## 一、提炼

**柯哥只做了一件事：让小龙「检查系统健康状况」。** 全天 136 条消息，除这一句外全是 cron 定时任务 + 小龙自动回复。

**这一查，查出一堆积压问题。** 健康检查报告显示：

1. **核心服务正常，但 cron 有 6/16 上次运行失败**——两个真问题（统一健康检查器模型 `deepseek/deepseek-chat` 不在 allowlist、新旧两套「每日对话备份」「每日精华」重复并存），四个瞬态（API 超时、限流、session pause）。
2. **安全网在漏。** 两道闸失效（真实产物过闸复验、后台任务健康），一道 guard 判据单元测试不通过（150 过 149）。每道闸背后都是一次真实事故的防复发机制。
3. **账对不上。** 影子对账 3 处不一致、MEMORY.md 从 6409 涨到 8805 字节（超容差）、compaction 参数漂移（keep=12000 vs 基线 8000）。
4. **活跃会话 356 个**，偏多，可能拖慢上下文。

小龙把「要我动手修吗」摆到柯哥面前，柯哥没回。

## 二、反思

**系统在靠「报警」而不是「修复」维持。** 昨天的精华还写着「待修 compaction 漂移、微信链路」，今天这些问题一个没解决，反而新增了安全网闸失效、guard 测试失败。报警机制是好的——它每天诚实地喊「哪里坏了」，但喊完没人动手，问题就一层层叠起来。

**柯哥只查不修，这本身是个信号。** 他不是不想修，是这些系统维护活不该占他的注意力。小龙该做的不是反复问「要我修吗」，而是把能自动修的自动修掉，把真正需要拍板的（compaction 参数漂移）单列出来等他一句话。

**重复 cron 是今天一切「空转」的根。** 两个每日对话备份、两个每日精华并存，才导致同一个任务一天被触发五六次、每次都读完大文件再判断「已存在」。漏跑补偿器全天 24 次 NO_ACTION，也是同类空转。

## 三、行动项

- [ ] 修统一健康检查器模型：`deepseek/deepseek-chat` → `deepseek/deepseek-v4-flash`（小龙自己引入的错误）
- [ ] 清理重复 cron：关掉旧的「每日对话备份」和旧的「每日精华」，保留新版
- [ ] compaction 参数漂移 → 柯哥拍板（脚本已标 NEEDS_HUMAN，勿自动回写）
- [ ] 两道安全网闸失效 + guard 判据测试失败 → 转 Claude 修
- [ ] 影子对账 3 处、MEMORY.md 体积超容差 → 转 Claude 处理
- [ ] 归档一批老会话，降低 356 活跃会话
