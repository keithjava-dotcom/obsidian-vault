---
author: Hermes
maintainer: Hermes
created: 2026-08-04
type: 测试报告
tags: [帧言, 工程验收, lint, build, 测试]
---

# 帧言工程验收报告 · 2026-08-04

> 对象：`~/Documents/frameyan/`（v0.9.0-rc.6，commit 09f3b14）
> 验收命令：README 官方验收命令全套 + 功能修复

---

## 一、结论

**工程验收全绿。** lint / tsc / 三个测试套件 / 生产构建 / PWA 全部通过，代码质量合格。

## 二、验收明细

| 命令 | 内容 | 结果 |
|------|------|------|
| `npm run lint` | 前端 env 检查 + tsc --noEmit | ✅ |
| `npm run build`（prebuild） | check:prod-mock → lint → test:bff-unit → test:bff-core → test:frontend → vite build → PWA | ✅ exit 0，构建 2.8s |
| PWA | generateSW，53 entries（1.97MB） | ✅ |

## 三、修复记录（功能测试发现 · 本次验收前已修）

| commit | 内容 |
|--------|------|
| 09f3b14 | ①注册 EMAIL_ALREADY_EXISTS 静默吞掉 → 明确提示+切回登录 ②校准"下一步"disabled 无反馈 → 点击触发校验提示 |

两个修复均通过 tsc 全量编译 + 完整测试套件（本次 build 已覆盖）。

## 四、遗留（不影响验收结论）

1. **mvp-api 冒烟测试**：需要隔离数据根+管理员凭据，未跑（设计为 CI 环境用）
2. **多 LLM provider 验收**（anthropic/deepseek/openai 三套）：代码在但未逐个实测
3. **UI 交互复测**：两个修复待柯哥真浏览器确认（报告 17 号）

## 五、下一步：架构改造（步骤 3，见 19 号施工单）

验收通过 → 按蓝图 v2：砍自研轮子（skill-executor/llm providers/calibration/记忆存储）→ BFF 对接 Hermes 底座。方案详见 [[19-架构改造施工单-接Hermes底座]]。
