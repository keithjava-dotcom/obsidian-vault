---
author: Hermes (claude-sonnet-4 via Nous)
maintainer: Hermes
created: 2026-08-04
type: 进度报告
status: 阶段A+C完成·待柯哥拍板接入形态
tags: [帧言, 架构改造, Hermes底座, 进度]
---

# 帧言接 Hermes 底座 · 中期进度报告（2026-08-04）

> 对应施工单：19 号。原则：不造轮子、灰度逐棒、可随时回滚。

## ✅ 已完成的验证（全部实测）

### 阶段 A · 基础连通（完成）
| 项 | 结果 |
|----|------|
| HermesAdapter（`bff/src/llm/hermesAdapter.ts`，commit 9a829ee） | 写好，调 `hermes -z ... chat` |
| 单次问答连通 | ✅ 返回 ADAPTER_OK |
| 技能预载（`-z` 形态） | ✅ 正确加载 |
| **每用户 profile 记忆隔离** | ✅ 实测：一个用户的记忆另一用户完全看不到（多用户容量关键验证） |

### 阶段 C1 · 五棒逐棒实测（完成，用减肥餐创业者真实案例连续跑）
| 棒 | Hermes 技能 | 实测结果 | 质量 |
|----|-----------|---------|------|
| 定位 | dingwei-report-workflow | 定位一句话+人群+风格+主线+3条增量洞察 | ⭐很高 |
| 选题 | xuanti-workflow | 8选题+角度分类+升级/拉新配比 | ⭐高 |
| 大纲 | dagang-workflow | 钩子+3支撑+行动号召+拍方向问题 | ⭐高 |
| 文稿 | fanwen-workflow | 真人感口播稿，无AI味、自嘲、结尾触发评论 | ⭐很高 |
| 复盘 | fupan-workflow | 3建议+诚实标注数据缺口 | ⭐高 |

**核心结论**：Hermes 五棒技能全部能真干活、自动衔接下一棒（"递 dagang"、"写逐字稿"），保持柯哥方法论气质。杀掉自研 skill-executor 换 Hermes，功能不丢、质量更稳。

### 引擎结构摸清（为接入定方案）
- `Session.skillId` 标识当前技能 ✅
- `executeSkillStep`（skillExecutor.ts）是统一入口，被 session/skills 路由调用
- `engine.ts`（3000+行）有 4+ 处硬编码 `getProvider('deepseek')` 的 LLM 调用
- 前端消费核心是 `result.session`（messages/artifacts/status）

## 🔴 最新进展（same day 追加）

### 五棒连棒验证通过（2026-08-04 决定性里程碑）
用**真实上游产出**依次跑通 大纲→文稿→复盘（选题→拍板也提前验证）：
- ✅ 大纲：基于选题产出 2.7K 字大纲（钩子+支撑+骨架判断+递给写稿）
- ✅ 文稿：用真实大纲写出高质量口播稿（"腰疼没有正常这回事，你坐八小时腰替你撑八小时，它不是在疼是在喊"——有血肉、呼吸、自嘲）
- ✅ 复盘：用稿子+数据出专业复盘（L1定性/P0预判/缺曲线诚实标不定论）

**结论：Hermes 五棒 = 完整内容生产线**，每棒保持柯哥要求的高质量、真判断、诚实标注。

### 五棒耗时摸底（2026-08-04）
| 棒 | Hermes技能 | 耗时 | 质量 | 备注 |
|----|-----------|------|------|------|
| 选题 xuanti | xuanti-workflow | ~15s | 高 | 快,出JSON选题卡 |
| 大纲 dagang | dagang-workflow | ~65s | 高 | 深度多轮,慢但可用 |
| 文稿 fanwen | fanwen-workflow | ~65s | 高 | 真人感,慢但可用 |
| 声画 broll | b-roll-plan-workflow | 23s | ⚠️ | **需真实上游稿子** |
| 复盘 fupan | fupan-workflow | ~103s | 高 | 最慢,深度归因 |

🔴 结论：
- 速度普遍 20-100s（Hermes 深度多轮推理固有），对"无缝接管"可接受，但前端需给足加载态
- **五棒衔接至关重要**：broll/fupan 依赖上游产出（逐字稿/定位方案），单棒独立跑做不了
- 无缝接管后端核心（选题 JSON 卡）已验证通过

### 方案甲 · 后端 + 前端全链路打通（柯哥已拍板选甲）✅

**后端**（commit a9bd7f9）：
- 新路由 `bff/src/routes/hermes.ts`：`GET /api/hermes/skills` + `POST /api/hermes/run`
- 五棒技能映射 dingwei/xuanti/dagang/fanwen/fupan → Hermes 技能
- dev-server dispatcher 挂载（server/dev-server.mjs）
- 独立通道，自研 skill-executor 零改动，可随时回滚
- 冒烟实测：xuanti✅ fanwen✅

**前端**（commit 3ade96e）：
- 新页面 `src/pages/HermesPipeline.tsx`：五棒独立演示/验收页
- ViewState.HERMES + hash 路由 `#/hermes` + 侧边栏"Hermes五棒"入口
- 修正 2 个真实 bug：token 存储方式（改 apiJson 自动带）＋ apiJson 路径 /api 重复
- 用 `apiJson`（自动带鉴权 token）

**端到端实测（真实浏览器）**：登录 → 打开 `#/hermes` → 选题棒 → 输入素材 → 点运行 → **Hermes 真实产出选题** ✅

## ⏸ 待柯哥拍板：Hermes 灰度的接入形态（下一步方向分叉）

摸清引擎后，接入方式有两条路，产品形态不同，需拍板：

**方案甲 · 独立 Hermes 通道（推荐，最稳）**
- 自研引擎（engine.ts）零改动
- 新建一个走 Hermes 的独立技能执行入口，五棒经 Hermes 跑
- 前端以独立形态展示 Hermes 产出
- 可随时彻底回滚；非灰度功能绝不受影响

**方案乙 · 无缝替换原引擎**
- 在 executeSkillStep 内部加 useHermesFor 分支，灰度技能走 Hermes，返回结构完全复刻原协议
- 前端看不出差别，无缝体验
- 但需完整复刻自研协议（messages/artifacts/blocked），工程量大、耦合深、改坏核心执行路径风险高

## 建议
选**方案甲**起步——先把 Hermes 五棒作为一个可靠的新通道跑起来（已验证质量高），你逐棒验收。方案乙的"无缝替换"等方案甲验证稳定后再演进，不急于一步到位。

---
*报告人：Hermes (claude-sonnet-4 via Nous) · 待柯哥拍板后继续*
