# 26-换内核进行时-20260805.md

> 状态：进行中（柯哥下班暂停，回家继续）
> 本次会话：换内核 = 解除 SkillRunner 表格式闸门 + 根治"肥柯没动静"

## 已完成并提交

### 1. 前端：解除主导标签页闸门（commit 0b4cf0b）
- `FySkillPanelBase.tsx` + `FyXuantiPanel.tsx`：启动/输入框/拍板按钮不再被 `isLeader` 卡住
- 文案微信化："柯建军在呢——说点什么，选题这就开始" / placeholder "跟柯建军说点什么..."
- 效果：用户打开 skill-runner 即自动 startSession（不再卡"成为主导标签页后自动启动"）

### 2. 后端：hermesAdapter 可靠性改造（未提交，工作区）
- 并发锁（同一时刻一个 hermes 子进程）
- 超时 180s→60s，超时自动重试一次，SIGKILL 强杀
- **根因已定位**：hermes CLI 是 venv Python 脚本，启动靠 PATH 解析自身依赖。
  dev-server 由 npm 启动时 PATH 只有 node_modules/.bin 链 → hermes 缺 venv/bin
  走 fallback 慢路径 → 卡 60s+ → 用户看到"没动静"。
- 修复：spawn 时 PATH 合并，**venv/bin 放最前**（hermesAdapter.ts 已改，tsc 通过）
- ⚠️ 但后端实测仍 60s 超时（手动 shell 秒回，后端 spawn 慢）——**最后一个差异未定位**。
  怀疑：残留 hermes 子进程并发（刚 kill 了一批）；或 cwd/env 差异。

## 下一步（回家继续）

1. 重启后端（`npm run dev:api`），确认加载 PATH 修复
2. 杀干净残留 hermes 子进程后，用 ADMIN 账号（ke_admin2_1785892865@test.com）测
   `POST /api/skills/run` 耗时——目标 <15s
3. 若仍慢：对比 spawn env（补 SHELL/HERMES_HOME 等），或改用 `hermes serve` 长驻（阶段 D 设计）
4. 测通后 commit hermesAdapter，浏览器端到端验证"打开即对话"
5. 更新 todo：T4（换内核）收口

## 关键文件
- `bff/src/llm/hermesAdapter.ts`（PATH 修复在此，未提交）
- `bff/src/routes/skills.ts`（加了 [hermes-run] 日志，未提交）
- `src/components/skill/FySkillPanelBase.tsx` / `FyXuantiPanel.tsx`（已提交 0b4cf0b）

## 服务状态
- 后端 8787 / 前端 3000 在跑（本机）
- 测试账号：ke_admin2_1785892865@test.com / Test12345!（ADMIN 不限量）
