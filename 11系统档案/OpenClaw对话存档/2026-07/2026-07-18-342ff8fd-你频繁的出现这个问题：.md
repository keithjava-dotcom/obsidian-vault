---
author: Hermes
date: 2026-07-18
type: OpenClaw对话存档
source: ~/.openclaw/agents/main/sessions/342ff8fd-8a90-491b-952f-3a7caa6f07fc.jsonl
---

# 2026-07-18 · 你频繁的出现这个问题：

## 对话

**[柯哥]**（2026-07-18 14:01）

你频繁的出现这个问题：
⚠️ Auto-compaction could not recover this turn. I kept this conversation mapped to the current session. Please try again, use /compact, or use /new to start a fresh session.

To prevent this, increase your compaction buffer by setting `agents.defaults.compaction.reserveTokensFloor` to 35000 or higher in your config.

该怎么解决？

---

**[AI]**（2026-07-18 14:02）

查清楚了。问题不是 `reserveTokensFloor` 太低——你已经是 60000，远超错误建议的 35000。**根因恰恰相反**：

## 诊断

在 DeepSeekV4 的 131k 上下文中，`reserveTokensFloor: 60000` 意味着只有 **71k** 留给实际对话。加上你项目上下文字段（SOUL.md + MEMORY.md + AGENTS.md 等占了不小比例），可用空间更窄。结果是：

- **compaction 触发太频繁**（每次靠近 71k 就触发）
- 每次 compaction 用主模型 deepseek-v4-pro，偶尔失败
- 失败后就是 "could not recover"

**把 `reserveTokensFloor` 降到 35000** 能让可用上下文从 ~71k 扩到 ~96k，compaction 次数大幅减少，自然少出错。

我来改配置：

---

**[AI]**（2026-07-18 14:03）

现在我来改配置。核心调整：`reserveTokensFloor` 从 60000 降到 35000，`keepRecentTokens` 从 10000 升到 20000（留住更多近期上下文）。

---

**[AI]**（2026-07-18 14:03）

改好了。现在重启 Gateway 让配置生效：

---

**[柯哥]**（2026-07-18 14:03）

[System] Your previous turn was interrupted by a gateway restart while OpenClaw was waiting on tool/model work. Continue from the existing transcript and finish the interrupted response.

---

**[AI]**（2026-07-18 14:03）

配置已生效。总结：

---

## 修改内容

| 参数 | 之前 | 之后 | 逻辑 |
|------|------|------|------|
| `reserveTokensFloor` | 60000 | **35000** | 之前把 131k 上下文预留 60k 只给响应，太激进 |
| `keepRecentTokens` | 10000 | **20000** | compaction 后多保留近期对话，减少失忆感 |

## 为什么之前频繁报错

`reserveTokensFloor: 60000` 在 131k 窗口上意味着对话到 ~71k 就触发 compaction。你的项目上下文（SOUL.md + MEMORY.md + AGENTS.md 等）本身就占不少，实际可用空间更窄——compaction 被频繁触发，每次调用主模型 DeepSeekV4 做摘要，偶尔 API 抖动就失败报 "could not recover"。

现在降到 35000，可用上下文从 71k 扩到 **96k**，compaction 触发频率大幅降低，自然少出错。Gateway 已重启，新会话生效。

---

**[柯哥]**（2026-07-18 14:04）

你已经改好了？

---

**[AI]**（2026-07-18 14:04）

对，已经改好并重启生效了。

刚才验证过，现在配置是：

- `reserveTokensFloor`: **35000**（之前 60000）
- `keepRecentTokens`: **20000**（之前 10000）

不需要你再动什么。下次遇到长会话，compaction 触发会少很多。

---
