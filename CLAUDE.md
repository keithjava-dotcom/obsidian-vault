# 肥柯的武器库：Claude 操作规则

本目录是正式 Obsidian vault。Obsidian 只承担本地文件仓库和人工查看；Claude 是外部执行层。

## 必须遵守

1. 任何 Obsidian 任务都先应用 `obsidian-vault-guard`；Markdown 语法任务同时应用 `obsidian-markdown`。
2. 写前读取源文件和目标文件，并检查 `git status --short`；不得声称读取了实际未读取的文件。
3. 原始资料、客户资料、产品手册和系统治理文件默认只读。派生摘要、索引、图谱与分析另建文件，并标明准确来源。
4. 未经柯哥明确授权，不得删除、移动、重命名、覆盖、批量改 frontmatter，也不得提交、推送或改变 iCloud/Git 同步设置。
5. 不得把一个客户目录的信息复制进另一个客户目录；不得写入密钥、Token、环境变量值或模型隐藏推演。
6. 每次修改后必须回读目标并检查精确 diff；只报告真实改动，不把局部验证说成全部完成。
7. Codex、Claude、Hermes 同时工作时只能有一个写入者。目标在读取后发生变化，立即停止并重新核对。
8. 未指定测试位置时，只能在 `99-测试沙盒/` 新建明确标注的测试文件。

## 高风险目录

`.git/`、`.obsidian/`、`.claude/`、`.claudian/`、`.smart-env/`、`01新媒体知识库-源文件/`、`04服务客户/`、`08学习源文件/`、`11系统档案/`、`12帧言产品手册/`、`skill-learning/`、`task-handoffs/`。

如果目标不清、源文件缺失、Git 有重叠改动或权限不足，必须停止并说明，不能脑补完成。
